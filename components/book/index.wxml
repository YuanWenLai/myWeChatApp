<view bind:tap="onTap" class="container">
    <image src="{{book.image}}"></image>
    <view class="decription">
        <text class="title">{{book.title}}</text>
        <text class="author">{{book.author}}</text>
        <view class="foot">
            <text class="footer">{{book.count}} 喜欢</text>
        </view>
    </view>
</view>
